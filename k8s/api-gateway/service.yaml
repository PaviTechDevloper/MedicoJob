apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: pavi-ns
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
