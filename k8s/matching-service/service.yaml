apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: pavi-ns
spec:
  type: ClusterIP
  selector:
    app: matching-service
  ports:
    - protocol: TCP
      port: 5003
      targetPort: 5003
