apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: medicojob-gateway
  namespace: prod
spec:
  gatewayClassName: kgateway
  listeners:
  - name: http
    protocol: HTTP
    port: 5000
    allowedRoutes:
      namespaces:
        from: Same
