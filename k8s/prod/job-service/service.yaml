apiVersion: v1
kind: Service
metadata:
  namespace: prod
  name: job-service
spec:
  type: ClusterIP
  selector:
    app: job-service
  ports:
    - protocol: TCP
      port: 5002
      targetPort: 5002

