apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: standard
provisioner: nfs.csi.k8s.io
parameters:
  server: [IP_ADDRESS]
  share: /var/lib/data
volumeBindingMode: Immediate
mountOptions:
  - nolock
