apiVersion: v1
kind: Service
metadata:
  namespace: dev
  name: reputation-service
spec:
  type: ClusterIP
  selector:
    app: reputation-service
  ports:
    - protocol: TCP
      port: 5006
      targetPort: 5006

