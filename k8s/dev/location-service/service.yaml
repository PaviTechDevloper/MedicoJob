apiVersion: v1
kind: Service
metadata:
  namespace: dev
  name: location-service
spec:
  type: ClusterIP
  selector:
    app: location-service
  ports:
    - protocol: TCP
      port: 5005
      targetPort: 5005

