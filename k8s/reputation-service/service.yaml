apiVersion: v1
kind: Service
metadata:
  name: reputation-service
  namespace: pavi-ns
spec:
  type: ClusterIP
  selector:
    app: reputation-service
  ports:
    - protocol: TCP
      port: 5006
      targetPort: 5006
