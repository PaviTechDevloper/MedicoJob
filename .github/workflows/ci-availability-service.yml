name: Availability Service Pipeline

on:
  pull_request:
    types: [opened, synchronize]
    branches: [main]
    paths:
      - "backend/availability-service/**"
      - ".github/**"
  push:
    branches: [main]
    paths:
      - "backend/availability-service/**"
      - ".github/**"
  workflow_dispatch:

concurrency:
  group: medicojob-availability-service-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipeline:
    uses: ./.github/workflows/ci_service_pipeline.yml
    with:
      service_name: availability-service
      service_path: backend/availability-service
      sonar_project_key: ${{ vars.SONAR_PROJECT_KEY_AVAILABILITY_SERVICE || vars.SONAR_PROJECT_KEY }}
    secrets: inherit
