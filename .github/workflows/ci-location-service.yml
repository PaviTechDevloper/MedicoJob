name: Location Service Pipeline

on:
  pull_request:
    types: [opened, synchronize]
    branches: [main]
    paths:
      - "backend/location-service/**"
      - ".github/**"
  push:
    branches: [main]
    paths:
      - "backend/location-service/**"
      - ".github/**"
  workflow_dispatch:

concurrency:
  group: medicojob-location-service-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipeline:
    uses: ./.github/workflows/ci_service_pipeline.yml
    with:
      service_name: location-service
      service_path: backend/location-service
      sonar_project_key: ${{ vars.SONAR_PROJECT_KEY_LOCATION_SERVICE || vars.SONAR_PROJECT_KEY }}
    secrets: inherit
