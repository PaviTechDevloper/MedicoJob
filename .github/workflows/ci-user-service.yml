name: User Service Pipeline

on:
  workflow_call:

concurrency:
  group: medicojob-user-service-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipeline:
    uses: ./.github/workflows/ci_service_pipeline.yml
    with:
      service_name: user-service
      service_path: backend/user-service
      sonar_project_key: medicojob-user-service
      sonar_token_secret_name: SONAR_TOKEN_USER_SERVICE
    secrets: inherit
